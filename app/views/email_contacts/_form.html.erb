
<div class="contactform span-11">

  <h2 class="contactform">Send me an email!</h2>
  
<%= form_for(@email_contact) do |f| %>
  <% if @email_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_contact.errors.count, "error") %> prohibited this email_contact from being saved:</h2>

      <ul>
      <% @email_contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit :value => "Send it!"%>
  </div>
<% end %>
</div>